# Backend API base URL (no trailing slash). Leave empty to use mock data in development.
NEXT_PUBLIC_API_BASE_URL=

# Set to "true" to always use mock data (e.g. for demos).
# NEXT_PUBLIC_USE_MOCK=true
